// src/instrumentation.ts
// Archivo de instrumentación para Sentry (opcional)
// Desactivado por defecto - habilitar cuando Sentry esté configurado

export async function register() {
  // Sentry desactivado por defecto - habilitar cuando esté listo
  // if (process.env.NEXT_PUBLIC_SENTRY_DSN && process.env.NEXT_RUNTIME === 'nodejs') {
  //   await import('../sentry.server.config');
  // }
}

